name: Schedule AI Blog Post

on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:

jobs:
  post-blog:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger blog generation
        env:
          VERCEL_CRON_URL: ${{ vars.VERCEL_CRON_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          if [ -z "$VERCEL_CRON_URL" ]; then
            echo "Missing VERCEL_CRON_URL repository variable."
            exit 1
          fi

          # The API auto-discovers trending AI topics via Google Search grounding.
          # Send an empty body to let it pick today's top story automatically.
          curl -s -X POST "$VERCEL_CRON_URL" \
            -H "Authorization: Bearer $CRON_SECRET" \
            -H "Content-Type: application/json" \
            -d '{}'
